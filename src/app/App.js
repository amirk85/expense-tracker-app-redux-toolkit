import Home from "../components/Home";

function App() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Home />
    </div>
  );
}

export default App;
